# PatchFlow 项目需求与技术设计文档

> 项目副标题：A Verifier-Guided Repository-Level Code Agent  
> 中文名称：验证驱动的仓库级代码智能体  
> 文档版本：0.1  
> 文档状态：设计草案  
> 目标读者：项目开发者、代码审查者、实验复现者、Code Agent / LLM 后训练岗位面试官  
> 当前范围：只定义需求、架构、协议、实验与验收标准，不包含实现代码

---

## 1. 文档目的

本文档用于完整定义 PatchFlow 的项目定位、功能边界、系统架构、核心算法、运行时隔离、评测方法、实验设计、工程规范和开发计划。

本文档需要解决以下问题：

1. PatchFlow 为什么值得做，它与普通聊天式编程助手有什么区别。
2. 项目需要实现哪些能力，哪些能力暂时不做。
3. Agent、工具、沙箱、代码索引、记忆、验证器和评测系统如何协作。
4. 如何避免项目停留在“LLM 调工具”的演示水平。
5. 如何通过 SWE-bench 和自建任务对项目进行可信、可复现的测评。
6. 如何设计 baseline、消融实验和失败分析，以支撑技术结论。
7. 如何把项目沉淀为可展示、可深挖、可扩展到训练研究的实习作品。

本文档是后续实现的最高层设计依据。实现过程中如果发生重要架构变更，应先更新本文档或新增架构决策记录，再修改代码。

---

## 2. 项目摘要

PatchFlow 是一个面向真实软件仓库问题修复的 Code Agent 系统。系统接收代码仓库、基础提交和 Issue 描述，在受控环境中自动完成问题理解、故障复现、代码定位、方案规划、候选补丁生成、测试验证、失败反思和最终补丁输出。

PatchFlow 同时提供三种使用形态：

1. **交互模式**：用户像使用终端编程助手一样与 Agent 协作。
2. **自主模式**：Agent 在给定预算内独立处理一个 Issue，并输出经过验证的补丁。
3. **批量评测模式**：在固定模型、预算和任务集合上运行 baseline、完整方法与消融实验。

项目的核心研究问题是：

> 在模型和推理预算固定的条件下，结构化故障定位、Evidence Graph 记忆、候选补丁分支搜索和分阶段验证，能否比普通线性 ReAct Agent 更准确、更稳定、更低成本地解决仓库级软件问题？

PatchFlow 不以复刻某个商业编程助手的界面为最终目标，也不以堆积工具数量作为技术亮点。项目重点是建立一个可观察、可复现、可评测、可扩展的 Code Agent 研究与工程平台。

---

## 3. 背景与问题定义

### 3.1 目标任务

一个标准任务由以下信息组成：

- 一个 Git 仓库或仓库快照。
- 一个明确的基础提交 `base_commit`。
- 一段自然语言问题描述，例如 GitHub Issue。
- 可选的公开测试命令、构建命令和环境说明。
- 对 Agent 不可见的最终评测规则或隐藏测试。

Agent 的最终产物是一个可以应用到基础提交上的 Git diff。成功不以“Agent 声称修复完成”为准，而以独立评测环境中的执行结果为准。

### 3.2 仓库级修复的主要困难

仓库级问题修复不同于单函数代码生成，主要困难包括：

- Issue 描述往往不完整，真实故障位置未知。
- 仓库中存在大量与任务无关的文件，不能全部放入上下文。
- 修复可能跨越多个文件、模块和调用层级。
- Agent 需要主动运行命令、读取错误栈并更新假设。
- 测试通过不一定等价于满足用户意图，可能存在 reward hacking。
- 长轨迹会引起上下文膨胀、重复探索和关键信息遗失。
- Shell 执行具有安全风险，且第三方项目依赖复杂。
- 不同模型、预算、环境和缓存设置会严重影响实验结果。

### 3.3 项目价值

PatchFlow 需要同时体现以下四类能力：

- **Agent 算法能力**：状态机、工具选择、反思、搜索和上下文管理。
- **软件工程能力**：模块化架构、类型系统、测试、错误处理和可扩展接口。
- **系统能力**：Docker 沙箱、超时、并发、资源限制和可观测性。
- **研究能力**：基线、消融、指标、统计、失败分析和复现实验。

---

## 4. 项目目标与非目标

### 4.1 核心目标

#### G1：完成真实仓库问题修复闭环

系统能够从 Issue 和仓库出发，输出可应用、可验证的补丁，而不是只提供自然语言建议。

#### G2：建立可靠的 Agent Harness

系统完整管理模型调用、工具调用、状态迁移、预算、重试、上下文和终止条件，并保存可回放轨迹。

#### G3：建立安全、可替换的 Runtime

Agent 不直接依赖宿主机环境。执行层支持本地开发环境和 Docker 隔离环境，并可适配 SWE-bench 实例。

#### G4：实现证据驱动的修复过程

Agent 的定位、计划和反思应基于 Issue、代码结构、错误栈、测试结果和历史尝试等显式证据，而不是只依赖未经整理的对话历史。

#### G5：实现验证驱动的候选搜索

系统能够生成多个候选补丁，在隔离分支中分阶段验证，并以可解释的规则选择最终补丁。

#### G6：建立可信评测体系

系统支持自建快速任务集和 SWE-bench 子集，能够在相同模型、预算和任务上比较不同 Agent 策略。

#### G7：形成可用于面试和研究扩展的成果

项目最终应包含完整 README、架构说明、实验报告、失败案例、轨迹样例和演示材料。

### 4.2 非目标

首个稳定版本不追求以下目标：

- 不复刻 Claude Code、Codex 或其他商业产品的全部交互体验。
- 不支持所有编程语言，第一阶段聚焦 Python 仓库。
- 不追求直接超过公开排行榜上的最强商业系统。
- 不在缺少可靠评测前进行大规模 RL 训练。
- 不默认采用多 Agent 架构。
- 不实现完整 IDE 插件、网页 IDE 或复杂前端。
- 不允许 Agent 无限制访问网络、宿主机或用户目录。
- 不把向量数据库、MCP、Skills 或 LangChain 当作项目核心贡献。

---

## 5. 成功标准

项目成功需要同时满足工程成功和实验成功。

### 5.1 工程成功标准

- 能够在全新环境中按照文档完成安装和启动。
- 能够处理本地任务和 SWE-bench 任务。
- 每次运行都有唯一 run ID、完整配置、轨迹、日志、补丁和评测结果。
- Docker 任务之间相互隔离，任务能够重置到基础提交。
- 工具参数经过结构化校验，所有命令都有超时和输出上限。
- Agent 在异常、超时、模型错误和 patch 冲突时能够产生明确状态。
- 核心模块有单元测试，关键闭环有集成测试。

### 5.2 实验成功标准

- 至少实现 One-shot、Linear ReAct 和 Bash-only 三个 baseline。
- 至少完成 Hybrid Localization、Evidence Graph、Candidate Branching 和 Verifier Pyramid 的逐项消融。
- 在同一任务、同一模型、同一预算下进行成对比较。
- 报告修复率、定位准确率、成本、时延、工具调用和失败类型。
- 所有子集公开实例 ID，所有实验配置可复现。
- 子集结果明确标注为 subset，不与完整榜单结果混淆。
- 至少给出 5 个成功案例和 5 个失败案例的轨迹分析。

### 5.3 面试展示成功标准

面试时应能在 10 分钟内完成以下演示：

1. 输入一个未见过的小型 Issue。
2. 展示 Agent 如何复现、定位、修改和验证。
3. 展示 Evidence Graph 或结构化轨迹。
4. 展示候选补丁之间的验证结果。
5. 展示最终 diff、测试报告和成本。
6. 展示 baseline 与完整方法的实验对比。

---

## 6. 用户角色与典型场景

### 6.1 研究与开发者

希望修改 Agent 策略、工具接口、上下文策略或验证器，并在固定任务集上测量效果。

### 6.2 普通仓库维护者

希望在本地仓库中输入 bug 描述，让 Agent 给出经过测试的候选补丁，但最终修改仍由用户确认。

### 6.3 实验运行者

希望批量运行多个配置，查看任务级结果、聚合指标和失败原因。

### 6.4 代码审查者或面试官

希望快速理解系统边界、关键设计选择、实验结论和已知限制，并能够回放某个任务的完整过程。

---

## 7. 术语定义

| 术语 | 定义 |
|---|---|
| Agent Harness | 管理模型、工具、状态、上下文、预算和轨迹的核心控制系统 |
| Runtime | 为 Agent 提供文件系统、命令执行、补丁和快照能力的受控环境 |
| Evaluation Harness | 独立应用最终补丁、运行标准测试并判定成功的评测系统 |
| Trajectory | 一次任务中所有状态、模型请求、动作、观察和结果组成的有序记录 |
| Observation | 工具执行后返回给 Agent 的结构化结果 |
| Evidence | 支持或反驳某个故障假设的事实 |
| Evidence Graph | 组织 Issue、代码、测试、假设、补丁和结果关系的结构化图 |
| Candidate Patch | 针对同一任务生成的一个候选修改方案 |
| FAIL_TO_PASS | 修复前失败、修复后预期通过的测试 |
| PASS_TO_PASS | 修复前通过、修复后仍必须通过的回归测试 |
| ACI | Agent-Computer Interface，即 Agent 与计算机环境交互的接口设计 |
| Resolve Rate | 被独立评测判定完整解决的任务比例 |
| Inference Phase | Agent 可与公开仓库环境交互并生成补丁的阶段 |
| Evaluation Phase | Agent 不再参与，由独立 Harness 对最终补丁评分的阶段 |

---

## 8. 研究假设

所有“亮点”都应对应可检验假设，而不是只作为功能存在。

### H1：混合定位优于纯文本搜索

Issue 文本、文本搜索、AST 符号、导入调用关系、traceback 和测试依赖的组合，能够提高 gold 修改文件和函数的 Recall@K，并减少无关文件读取。

### H2：Evidence Graph 优于原始对话记忆

在相同上下文预算下，结构化记录事实、假设、尝试和反证，能够减少重复工具调用、减少重复失败并提高长轨迹任务的成功率。

### H3：候选分支搜索提高 Pass@1

在合理增加推理和测试预算的前提下，生成多个相互独立的候选补丁并进行执行验证，比单路径连续修补具有更高的最终修复率。

### H4：分阶段验证提高效率和可靠性

先运行低成本语法与目标测试，再运行高成本回归测试，可以更快淘汰无效候选，并降低仅针对单个测试过拟合的比例。

### H5：显式状态机提高可控性

将复现、定位、计划、修补和验证建模为显式阶段，相比完全自由的 ReAct 循环，更容易控制预算、分析失败和复现实验。

### H6：复杂工具并非始终优于 Bash-only

结构化工具可能提高安全性和可观测性，但强模型也可能更擅长直接使用 Shell。项目应在同一模型和预算下实证比较，而不是预设结论。

---

## 9. 需求优先级

需求分为三个优先级：

- **P0**：最小可用研究系统必须具备。
- **P1**：形成完整项目和主要技术亮点必须具备。
- **P2**：增强产品体验或支持未来研究，可延后实现。

版本范围建议：

| 版本 | 目标 | 包含范围 |
|---|---|---|
| MVP | 跑通可信闭环 | P0 |
| V1 | 完成核心研究与公开展示 | P0 + P1 |
| V2 | 扩展训练、协议和多语言能力 | 部分 P2 |

---

## 10. 功能需求

### 10.1 任务管理需求

#### FR-TASK-001（P0）：统一任务描述

系统必须用统一 TaskSpec 表示本地任务、自建任务和 SWE-bench 任务。

TaskSpec 至少包含：

- 任务唯一标识。
- 任务来源。
- 仓库来源或本地路径。
- 基础提交。
- Issue 标题和正文。
- 主要编程语言。
- 可选安装、构建和公开测试命令。
- Agent 最大步数、时间、token 和成本预算。
- 允许修改和禁止修改的路径约束。
- 与最终评测有关但对 Agent 不可见的元数据引用。

#### FR-TASK-002（P0）：任务生命周期

任务状态至少包括：待运行、环境准备中、运行中、已生成补丁、评测中、成功、失败、基础设施错误、取消。

#### FR-TASK-003（P0）：可重复运行

同一 TaskSpec、模型配置、随机种子和 Agent 配置应能够再次运行。外部模型不能保证逐 token 完全确定时，系统仍必须保存所有影响结果的可见配置。

#### FR-TASK-004（P1）：任务标签

任务可以标注仓库、难度、修改文件数量、问题类型、测试类型和是否需要多文件修改，以支持分层采样和失败分析。

### 10.2 模型接入需求

#### FR-LLM-001（P0）：统一模型接口

核心 Agent 不应绑定单个模型厂商。模型层必须抽象消息输入、结构化工具定义、生成参数、响应、token 用量、费用和错误。

#### FR-LLM-002（P0）：调用记录

每次模型调用必须记录：

- 模型标识和版本信息。
- 请求时间和响应时间。
- 可见消息或消息摘要。
- 工具定义版本。
- 温度、最大输出等参数。
- 输入 token、输出 token和缓存 token。
- 估算费用。
- 原始响应引用和解析状态。

#### FR-LLM-003（P0）：错误与重试

系统应区分限流、超时、网络错误、服务错误、上下文超限、无效结构化输出和安全拒绝。重试必须有次数上限和退避策略，并计入总预算。

#### FR-LLM-004（P1）：能力适配

系统应允许同一 Agent 策略适配原生 tool calling、纯文本动作协议和 Bash-only 三种模型交互方式。

### 10.3 Agent 控制需求

#### FR-AGENT-001（P0）：显式状态机

自主模式必须基于显式状态机运行，不允许只有一个不可观察的无限循环。

#### FR-AGENT-002（P0）：全局预算

系统必须同时限制：

- 最大 Agent 步数。
- 最大模型调用次数。
- 最大输入和输出 token。
- 最大工具调用次数。
- 最大命令运行总时长。
- 最大任务墙钟时间。
- 可选最大 API 成本。

任一硬限制触发时，Agent 必须停止并给出明确原因。

#### FR-AGENT-003（P0）：终止条件

Agent 只可在以下情况下终止：

- 产生至少一个候选补丁并通过要求的内部验证。
- 判断任务信息不足或环境不可用，并给出证据。
- 达到预算上限。
- 遭遇不可恢复的基础设施或模型错误。
- 用户在交互模式中取消。

#### FR-AGENT-004（P1）：策略可替换

One-shot、Linear ReAct、Bash-only 和 PatchFlow 完整策略应共享任务、模型、Runtime 和轨迹接口，以保证比较公平。

### 10.4 工具需求

#### FR-TOOL-001（P0）：结构化工具协议

每个工具必须声明名称、用途、输入模式、输出模式、权限级别、是否只读、默认超时和最大输出长度。

#### FR-TOOL-002（P0）：核心工具集

V1 核心工具控制在以下范围：

| 工具 | 作用 | 权限 |
|---|---|---|
| repo_map | 获取经过压缩的目录与模块结构 | 只读 |
| search_text | 在工作区内搜索字符串或正则 | 只读 |
| find_symbol | 按函数、类或限定名搜索符号 | 只读 |
| read_code | 按行或符号读取受限代码片段 | 只读 |
| apply_patch | 应用统一 diff，并返回修改摘要 | 写入 |
| git_diff | 查看当前修改 | 只读 |
| run_tests | 运行受控测试命令并解析结果 | 执行 |
| run_command | 执行经过策略检查的一般命令 | 高权限执行 |

#### FR-TOOL-003（P0）：输出约束

工具输出必须结构化，并明确表示成功、失败、超时、截断和权限拒绝。空输出也必须显式说明命令成功但无输出。

#### FR-TOOL-004（P0）：路径约束

所有文件工具必须把路径解析到工作区真实路径，并阻止 `..`、绝对路径、符号链接或挂载点造成的目录逃逸。

#### FR-TOOL-005（P1）：编辑后检查

应用补丁后应自动执行最低成本的 patch 完整性和语法检查。失败补丁不得悄悄保留在主候选工作区。

#### FR-TOOL-006（P1）：Observation 压缩

搜索、文件和命令输出必须支持行数限制、字节限制、头尾保留和结果摘要，避免一次工具调用耗尽上下文。

### 10.5 Runtime 与沙箱需求

#### FR-RUN-001（P0）：统一 Runtime 接口

Agent 只能通过 Runtime 使用文件、Git 和命令能力，不能直接依赖本地 `subprocess` 或 Docker SDK。

#### FR-RUN-002（P0）：本地 Runtime

提供面向开发和单元测试的 LocalRuntime，但默认只允许在明确指定的临时工作区中执行。

#### FR-RUN-003（P0）：Docker Runtime

正式任务使用容器隔离。每个任务拥有独立工作区，任务结束后可以清理或保留快照用于调试。

#### FR-RUN-004（P0）：执行约束

所有命令必须具备：

- 工作目录限制。
- 单次超时。
- 总执行时间限制。
- stdout/stderr 上限。
- 退出码和信号记录。
- 进程组终止，防止子进程残留。

#### FR-RUN-005（P1）：资源限制

Docker Runtime 应支持 CPU、内存、PID、磁盘和网络限制。默认禁止外网，并以非 root 用户运行。

#### FR-RUN-006（P1）：候选快照

每个候选补丁必须在独立 Git worktree、容器副本或等价快照中执行，候选之间不得共享未提交修改。

### 10.6 仓库分析与定位需求

#### FR-LOC-001（P0）：基础仓库地图

系统应生成过滤后的目录树、文件类型、关键配置、测试目录和主要包结构，并排除 `.git`、缓存、构建产物和大型二进制文件。

#### FR-LOC-002（P1）：符号索引

Python 首版至少索引模块、类、函数、方法、导入关系、定义位置和文档字符串摘要。

#### FR-LOC-003（P1）：混合定位

定位器应组合以下证据：

- Issue 中的路径、标识符、异常和关键词。
- 全仓文本搜索。
- traceback 文件和行号。
- 测试名称与模块关系。
- AST 符号和导入调用邻域。
- 已失败候选产生的新证据。

#### FR-LOC-004（P1）：可解释排序

每个可疑文件和符号都应记录分数、排名和证据来源。排序权重必须进入实验配置，不能硬编码后无法追踪。

#### FR-LOC-005（P1）：定位评测

对具备 gold patch 的任务，系统应计算 File Recall@K 和 Function Recall@K。由于 gold patch 不一定是唯一正确修复，还应在报告中注明该指标的局限。

### 10.7 Evidence Graph 需求

#### FR-EVG-001（P1）：节点类型

Evidence Graph 至少支持以下节点：

- Issue Fact：从问题描述抽取的事实和约束。
- File 与 Symbol：代码位置和结构。
- Test：测试标识、状态和耗时。
- Failure：异常、断言、退出码或超时。
- Stack Frame：错误栈中的位置。
- Hypothesis：关于根因和修复方式的可检验判断。
- Plan Step：计划中的具体步骤。
- Patch Attempt：某次候选补丁。
- Verification Result：某层验证的结果。
- Decision：选择、拒绝或回退某候选的理由。

#### FR-EVG-002（P1）：边类型

至少支持：属于、定义、导入、调用、失败于、覆盖、支持、反驳、修改、导致、验证、替代和派生关系。

#### FR-EVG-003（P1）：证据溯源

每条证据必须能够追溯到 Issue 文本、代码位置、工具调用、模型推断或测试结果。模型推断不得伪装成执行事实。

#### FR-EVG-004（P1）：置信度与状态

Hypothesis 应包含置信度、支持证据、反对证据和状态。状态至少包括待验证、部分支持、已确认、已反驳和已放弃。

#### FR-EVG-005（P1）：重复检测

系统应识别重复搜索、重复文件读取和已被反驳的相同修复方案，并在下一轮上下文中提示 Agent。

### 10.8 上下文管理需求

#### FR-CTX-001（P0）：上下文分区

上下文至少分为：

- 固定区：Issue、约束、预算和工具规则。
- 当前状态区：阶段、计划和待解决问题。
- 代码证据区：最相关代码片段和符号关系。
- 失败证据区：当前测试失败、错误栈和反证。
- 尝试记忆区：已尝试修改和结果。
- 最近交互区：最近若干动作与观察。

#### FR-CTX-002（P1）：预算分配

ContextBuilder 必须在总 token 预算内为不同分区分配容量，并记录哪些内容被选择、压缩或丢弃。

#### FR-CTX-003（P1）：证据选择

候选上下文的选择可综合重要性、新鲜度、与当前假设的相关性、可信度和 token 成本。

#### FR-CTX-004（P1）：压缩不丢失关键状态

上下文压缩后必须保留 Issue 约束、当前失败、已修改文件、关键假设和已失败方案。完整历史应存储在轨迹中，而不是永久丢弃。

### 10.9 候选补丁需求

#### FR-PATCH-001（P0）：标准补丁产物

最终修改以相对于 `base_commit` 的统一 Git diff 表示。系统不得只保存修改后的完整仓库而缺少标准 patch。

#### FR-PATCH-002（P1）：候选多样性

Candidate Branching 模式应允许生成多个候选。候选应优先体现不同根因假设或修复策略，而不是只改变措辞的重复补丁。

#### FR-PATCH-003（P1）：候选隔离

每个候选必须从同一基线或明确父候选创建，拥有独立修改、测试结果和轨迹。

#### FR-PATCH-004（P1）：最小修改原则

系统记录修改文件数、增加删除行数、测试文件修改和配置修改。补丁范围过大时应增加风险标记，但不能仅凭行数否定必要的大修改。

#### FR-PATCH-005（P1）：回滚

失败候选必须能够完全回滚，且不能污染后续候选或最终输出。

### 10.10 验证需求

#### FR-VER-001（P0）：验证金字塔

验证按从低成本到高成本执行：

1. patch 是否成功应用。
2. 修改文件是否可解析或编译。
3. 可选静态检查。
4. Agent 生成或指定的复现测试。
5. 与修改模块相关的目标测试。
6. 更大范围回归测试。
7. 完整测试或官方最终评测。

#### FR-VER-002（P0）：硬拒绝条件

以下候选应默认拒绝：

- patch 无法应用。
- 语法或编译失败。
- 明确的目标测试仍失败。
- 已有通过测试出现回归。
- 修改禁止路径。
- 删除、跳过或弱化关键测试来制造通过。
- 引入明显的硬编码答案或危险命令。

#### FR-VER-003（P1）：候选排序

排序首先依据确定性执行结果，再依据回归范围、修改风险、Issue 覆盖和成本。LLM Judge 只能作为缺少确定性信号时的辅助或平局决策，不能覆盖失败测试。

#### FR-VER-004（P1）：测试选择

系统应记录为什么选择某个测试：Issue 指定、失败栈相关、导入依赖相关、修改路径相关或完整回归。

#### FR-VER-005（P1）：防止评测投机

验证器需要检查测试篡改、异常吞噬、环境检测分支、仅匹配样例输入和跳过执行等常见 reward hacking 模式。

### 10.11 轨迹与事件需求

#### FR-TRAJ-001（P0）：追加式事件流

系统运行过程使用追加式事件记录。已经产生的事件不得原地篡改；派生摘要应保留来源事件 ID。

#### FR-TRAJ-002（P0）：事件种类

至少记录任务状态变化、模型请求与响应、工具调用与结果、预算变化、证据变化、候选创建、补丁应用、验证结果、上下文压缩和最终决策。

#### FR-TRAJ-003（P0）：可回放

轨迹浏览器能够按时间顺序展示 Agent 看到了什么、做了什么、环境返回了什么，以及为什么进入下一状态。

#### FR-TRAJ-004（P1）：训练数据导出

轨迹可导出为 SFT 样本、候选偏好对和 verifier 样本，但导出逻辑属于数据层，不得改变原始轨迹。

### 10.12 运行模式需求

#### FR-MODE-001（P0）：自主修复模式

输入任务后自动运行到终止状态，输出补丁、报告和轨迹。

#### FR-MODE-002（P1）：交互模式

用户可以查看计划、批准高风险操作、补充信息、要求回滚和选择候选。交互模式中的用户干预必须写入轨迹。

#### FR-MODE-003（P0）：批量评测模式

能够对任务清单和配置矩阵批量运行，支持有限并发、断点续跑、单任务重试和结果聚合。

#### FR-MODE-004（P2）：MCP 与 Skills

允许通过适配器加载 MCP 工具和项目 Skills，但外部工具必须经过相同权限、日志和预算系统。MCP 与 Skills 不得绕过 Runtime 安全边界。

---

## 11. 非功能需求

### 11.1 可复现性

- 配置必须版本化并随 run 保存。
- 保存代码版本、Agent 版本、模型名称、数据集版本、任务 ID和容器镜像标识。
- 每次批量实验生成独立 run ID。
- 结果缓存不得只依赖任务 ID；至少应包含 Agent 配置和 patch 或配置摘要。
- 如果外部模型不可完全确定，报告必须说明这一限制。

### 11.2 可测试性

- 领域模型不得依赖真实模型或 Docker 才能测试。
- 模型、Runtime、时钟和费用计算应可替换为 fake。
- Agent 状态迁移应能用确定性脚本模型测试。
- 每个工具同时具备正常、失败、超时和边界测试。

### 11.3 可观察性

- 日志使用结构化字段，而不只保存拼接文本。
- 所有事件带 run ID、task ID、candidate ID 和时间戳。
- 能够从一个失败任务定位到具体模型调用、工具调用和容器日志。
- 日志不得泄漏 API key、访问令牌和环境敏感变量。

### 11.4 可扩展性

- 增加模型、工具、Runtime、定位器、验证器和数据集时，不应修改核心领域对象的大量代码。
- Python 首版之后可通过语言适配器支持其他语言。
- 评测数据源与 Agent 控制逻辑解耦。

### 11.5 性能

- 仓库索引应可缓存，并以仓库提交为缓存键。
- 只读索引和候选验证可以受控并发。
- 同一任务的候选应共享不可变仓库索引，但不能共享可变工作区。
- 大型命令输出在进入持久化和上下文前必须截断或外置存储。

### 11.6 可维护性

- 核心接口有稳定语义和清晰类型。
- 领域层不依赖 CLI、具体模型 SDK 或 SWE-bench。
- 重大设计选择使用 ADR 记录。
- 代码覆盖率只作为辅助指标，关键行为测试优先于追求数字。

---

## 12. 总体系统架构

系统分为八层：

1. **Interface Layer**：CLI、交互界面、批量入口。
2. **Application Layer**：任务编排、运行控制、实验编排。
3. **Agent Layer**：状态机、策略、规划、反思和终止决策。
4. **Context and Memory Layer**：Evidence Graph、上下文构建和轨迹。
5. **Repository Intelligence Layer**：仓库地图、符号索引和故障定位。
6. **Tool and Runtime Layer**：工具分发、文件、Git、测试、Shell 和沙箱。
7. **Verification Layer**：验证金字塔、候选评分和最终选择。
8. **Evaluation Layer**：本地任务、SWE-bench adapter、指标和实验报告。

依赖方向必须由上层指向抽象接口，基础设施实现不得反向侵入 Agent 领域逻辑。

### 12.1 推荐目录边界

建议模块划分如下，最终名称可以调整：

```text
patchflow/
├── cli/                 交互、自主和批量运行入口
├── application/         任务与实验编排
├── domain/              Task、State、Event、Patch 等核心模型
├── agent/               状态机、策略、规划与反思
├── llm/                 模型抽象、消息和厂商适配器
├── tools/               工具定义、注册和分发
├── runtime/             Local、Docker、SWE-bench Runtime
├── repository/          仓库地图、AST、符号图和定位器
├── memory/              Evidence Graph、尝试记忆和上下文构建
├── verification/        验证器、测试选择和候选排序
├── evaluation/          数据集 adapter、runner、metrics 和 report
├── storage/             SQLite、JSONL、artifact 和缓存
└── observability/       日志、token、成本和 tracing
```

该目录只是职责设计，不代表必须一次创建全部空文件。实现时应按里程碑逐步生长。

---

## 13. 核心领域模型

本节只描述语义和字段，不规定具体实现代码。

### 13.1 TaskSpec

代表一个不可变的任务定义。

关键字段：

- `task_id`：稳定唯一标识。
- `source`：local、micro_swe、swe_bench 等。
- `repo_spec`：仓库地址、本地快照或镜像引用。
- `base_commit`：任务起点。
- `problem_statement`：Agent 可见问题描述。
- `language`：首版为 Python。
- `public_commands`：Agent 可以使用的安装和测试提示。
- `path_policy`：允许、禁止和只读路径。
- `budget`：任务预算。
- `tags`：实验分层标签。
- `evaluation_ref`：仅评测层可解析的元数据引用。

### 13.2 AgentState

代表某个时刻 Agent 的可恢复状态。

关键字段：

- 当前阶段和阶段内子状态。
- 当前计划与未完成步骤。
- 已使用预算和剩余预算。
- 当前主假设和备选假设。
- 已访问文件、符号和测试。
- 已创建候选及状态。
- 最近关键失败。
- Evidence Graph 的版本引用。
- 当前选中候选。
- 停止原因。

AgentState 是事件折叠后的当前视图，原始事实以事件流为准。

### 13.3 Event

所有事件共享以下元数据：

- `event_id`。
- `run_id`、`task_id`、可选 `candidate_id`。
- 事件类型和 schema 版本。
- 墙钟时间和单调时间。
- 产生者：Agent、Tool、Runtime、Verifier、User 或 Evaluator。
- payload 引用。
- 关联事件和因果父事件。

### 13.4 ToolCall 与 ToolResult

ToolCall 描述工具名称、校验后的参数、权限、请求阶段和调用 ID。

ToolResult 描述状态、结构化数据、stdout/stderr 引用、退出码、耗时、截断信息、错误分类和环境变化摘要。

### 13.5 CandidatePatch

关键字段：

- 候选 ID 和父候选 ID。
- 对应根因假设和修复策略。
- patch 内容引用与摘要。
- 修改文件和行数统计。
- 独立工作区引用。
- 各验证层结果。
- 风险标记。
- 最终状态：生成中、无效、验证中、通过、拒绝、选中。

### 13.6 VerificationResult

关键字段：

- 验证层级和验证器版本。
- 执行命令或检查规则。
- 通过、失败、跳过、超时或基础设施错误。
- 测试总数、通过数和失败数。
- 新增失败与已修复失败。
- stdout/stderr 和报告引用。
- 耗时与资源消耗。
- 失败原因和可反馈给 Agent 的摘要。

### 13.7 RunManifest

每次运行的总清单，至少包含：

- 代码版本和配置摘要。
- TaskSpec 引用。
- Agent、模型、Runtime、工具和验证器版本。
- 数据集与镜像版本。
- 时间、主机能力摘要和随机种子。
- artifact 路径。
- 最终状态和评测状态。

---

## 14. Agent 状态机设计

### 14.1 状态总览

```text
INITIALIZE
    ↓
UNDERSTAND
    ↓
REPRODUCE
    ↓
LOCALIZE
    ↓
PLAN
    ↓
GENERATE_CANDIDATES
    ↓
VERIFY_CANDIDATES
    ├── 有合格候选 → SELECT_AND_FINALIZE
    └── 无合格候选 → REFLECT
                         ├── 重新定位 → LOCALIZE
                         ├── 修订方案 → PLAN
                         ├── 继续修补 → GENERATE_CANDIDATES
                         └── 无可行路径 → STOP_FAILED
```

任意状态都可能因为取消、预算耗尽或不可恢复错误进入终止状态。

### 14.2 INITIALIZE

入口条件：TaskSpec 已通过校验。

主要动作：

- 创建 run manifest。
- 准备 Runtime 和干净仓库。
- 确认基础提交和工作区状态。
- 建立或加载仓库索引。
- 初始化预算、事件流和 Evidence Graph。

成功出口：环境可用，仓库处于预期基础提交。

失败出口：仓库不存在、提交无效、依赖环境不可构建或基础设施错误。

### 14.3 UNDERSTAND

目标：把 Issue 转换为明确的行为预期、约束和未知项。

输出至少包括：

- 问题摘要。
- 预期行为与实际行为。
- 明示路径、符号、异常和测试。
- 潜在问题类型。
- 需要验证的未知项。
- 初始复现计划。

自然语言推断必须标记为 hypothesis，Issue 原文事实标记为 fact。

### 14.4 REPRODUCE

目标：尽可能得到修复前可重复的失败信号。

优先顺序：

1. 使用 Issue 中明确给出的命令。
2. 运行相关已有测试。
3. 根据 Issue 构造最小复现脚本或测试，但不得覆盖已有测试。
4. 如果无法复现，记录原因并继续静态定位，降低最终置信度。

输出包括失败测试、异常类型、错误栈、环境条件和最小复现结果。

复现失败不一定终止任务，因为部分 Issue 依赖隐藏测试；但不得伪造“已复现”。

### 14.5 LOCALIZE

目标：产生有排序、有证据的可疑文件和符号集合。

步骤建议：

1. 从 Issue 和失败中抽取检索词。
2. 执行路径、符号和文本搜索。
3. 读取有限的高排名片段。
4. 扩展相关调用、导入或测试邻域。
5. 生成并排序根因假设。

阶段退出时应满足：至少存在一个可执行验证的根因假设，或者明确说明定位失败原因。

### 14.6 PLAN

目标：将根因假设转换为有限、可验证的修改方案。

每个方案至少说明：

- 对应哪个假设。
- 预计修改哪些符号。
- 为什么能修复问题。
- 可能影响哪些行为。
- 需要运行哪些验证。
- 出现什么结果时应放弃该方案。

计划不应细化成无意义的逐行叙述，也不能只是“修改代码并测试”。

### 14.7 GENERATE_CANDIDATES

目标：基于一个或多个方案生成独立候选补丁。

候选多样性优先来自：

- 不同根因假设。
- 不同修复层级，例如调用方与被调用方。
- 最小修复与更通用修复。
- 不同兼容性权衡。

系统应对语义重复候选进行去重。候选数量由实验配置和剩余预算决定。

### 14.8 VERIFY_CANDIDATES

目标：使用验证金字塔逐步淘汰无效候选。

候选验证可以并发，但必须：

- 使用独立可变工作区。
- 限制总并发和资源。
- 保持输出与候选 ID 对齐。
- 避免完整测试集同时争抢全部资源。

### 14.9 REFLECT

Reflection 不是让模型泛泛“反思一下”，而是结构化回答：

- 哪个预期被测试结果否定。
- 新出现了什么事实。
- 当前假设应确认、降权还是反驳。
- 失败属于定位错误、修复错误、验证不足还是环境错误。
- 下一步应重新定位、修改计划、缩小补丁还是停止。
- 是否正在重复已经失败的动作。

Reflection 的输出写回 Evidence Graph，并影响下一轮 ContextBuilder。

### 14.10 SELECT_AND_FINALIZE

目标：选择证据最充分的候选并生成最终产物。

最终产物包括：

- 标准 Git diff。
- 修改摘要。
- 内部验证报告。
- 已知风险和未运行测试。
- 完整轨迹引用。
- token、费用、工具次数和耗时。

“内部验证通过”不等于 SWE-bench 最终 resolved，报告必须区分两者。

---

## 15. 故障定位设计

### 15.1 多阶段定位

定位分为三个粒度：

1. **文件级定位**：找出最可能相关的若干文件。
2. **符号级定位**：定位类、函数或方法。
3. **编辑位置定位**：确定需要阅读和可能修改的行范围。

每一级都保留 Top-K 候选，不应过早只保留一个位置。

### 15.2 证据通道

建议首版实现以下可解释通道：

| 通道 | 典型证据 | 优点 | 局限 |
|---|---|---|---|
| Issue lexical | 文件名、符号、错误文本 | 快速、便宜 | 描述可能不准确 |
| Repository search | 精确字符串和正则命中 | 可定位真实代码 | 同名噪声多 |
| Traceback | 文件、行号、调用链 | 强动态证据 | 错误位置未必是根因 |
| Test relation | 测试导入和测试名称 | 与行为接近 | 测试结构可能复杂 |
| Symbol graph | 定义、调用、导入邻域 | 支持跨文件定位 | 静态调用图不完备 |
| Attempt feedback | 某候选造成的测试变化 | 直接反映因果 | 成本较高 |

### 15.3 排名与校准

定位分数由配置中的可解释特征组合得到。V1 不要求训练排序模型，但必须保存每个特征的原始值和最终贡献。

排序器需要支持：

- 不同证据通道开关。
- 权重配置。
- Top-K 设置。
- 文件和符号分开评估。
- 根据新 traceback 或测试反馈增量更新。

### 15.4 索引缓存

仓库索引缓存键至少包含仓库标识、基础提交、语言适配器版本和索引配置。工作区补丁产生后，可对修改文件进行增量更新，但基础索引不得被候选之间相互污染。

---

## 16. Evidence Graph 详细设计

### 16.1 设计动机

线性消息历史把事实、猜测、失败和冗长输出混在一起。长任务中模型容易遗忘关键约束、重复搜索，或者继续采用已经被测试反驳的方案。

Evidence Graph 的目标不是构建通用知识图谱，而是为当前任务维护一个小型、可溯源、可查询的工作记忆。

### 16.2 事实与推断分离

系统必须区分：

- **Observed**：由 Issue 原文、文件内容或执行结果直接得到。
- **Derived**：由确定性分析得到，例如 AST 导入关系。
- **Inferred**：由模型推断得到，例如“这里可能漏判空值”。
- **Evaluated**：由验证结果确认或反驳。

不同来源具有不同默认可信度。模型推断只有在获得执行证据后才可提升为确认状态。

### 16.3 生命周期

1. UNDERSTAND 阶段创建 Issue Fact。
2. REPRODUCE 阶段加入 Failure、Test 和 Stack Frame。
3. LOCALIZE 阶段连接 File、Symbol 与证据。
4. PLAN 阶段创建 Hypothesis 和 Plan Step。
5. PATCH 阶段创建 Patch Attempt 与修改关系。
6. VERIFY 阶段写入 Verification Result。
7. REFLECT 阶段更新假设状态并生成 Decision。

### 16.4 上下文投影

模型不直接接收完整图。ContextBuilder 根据当前阶段，把图投影为紧凑视图：

- 当前最高优先级假设。
- 每个假设最强支持和反对证据。
- 相关符号与有限代码片段。
- 最近失败候选和失败原因。
- 尚未验证的关键问题。

### 16.5 质量检查

Evidence Graph 应具备以下一致性检查：

- 所有执行事实都有事件来源。
- 已反驳假设不能继续作为主假设而没有新证据。
- patch 与实际 diff 中修改的文件一致。
- 测试结果和对应候选工作区一致。
- 删除节点采用失效标记而不是破坏历史。

---

## 17. 候选搜索与选择设计

### 17.1 搜索策略

V1 采用有限宽度的候选搜索，而不是无限树搜索。

推荐默认配置：

- 每轮最多 2 至 4 个候选。
- 最多 2 至 3 轮反思与重生成。
- 候选首先并行执行便宜验证。
- 只有排名靠前且通过便宜验证的候选进入完整回归。

### 17.2 候选生成来源

- 同一模型多次采样。
- 同一模型针对不同明确假设生成。
- 可选使用不同角色提示，例如最小修复和兼容性修复。
- 后续可加入检索到的历史成功模式，但必须防止任务答案泄漏。

### 17.3 选择原则

选择过程采用“硬约束优先、证据评分其次”的层级规则：

1. 淘汰无法应用和无法解析的补丁。
2. 淘汰目标行为仍失败的补丁。
3. 淘汰引入明确回归的补丁。
4. 在剩余候选中比较 Issue 覆盖、测试范围、修改风险和成本。
5. 确定性信号相同情况下，才允许辅助模型做代码审查。

### 17.4 不确定性表达

如果没有候选通过充分验证，系统不得把“最不差候选”标记为已验证成功。可以输出低置信度建议补丁，但必须在状态和报告中清楚区分。

---

## 18. 验证系统详细设计

### 18.1 验证层级

| 层级 | 检查内容 | 典型成本 | 是否阻断 |
|---|---|---:|---|
| V0 | patch 格式、路径和应用 | 极低 | 是 |
| V1 | 语法、导入或编译 | 低 | 是 |
| V2 | lint、类型或静态风险 | 低到中 | 配置决定 |
| V3 | 最小复现测试 | 中 | 通常是 |
| V4 | 目标模块测试 | 中 | 是 |
| V5 | 相关回归测试 | 中到高 | 是 |
| V6 | 完整公开测试 | 高 | 视预算决定 |
| V7 | 官方隐藏评测 | 最高 | 最终判定 |

### 18.2 测试结果语义

必须区分：

- 真实测试失败。
- 测试收集失败。
- 环境依赖失败。
- 命令超时。
- 容器被资源限制终止。
- 输出无法解析。
- 测试未运行。

只有真实执行且满足判定规则才能标记为通过。

### 18.3 回归保护

内部验证至少保存修复前测试基线和修复后结果。对于原本就失败的测试，不能错误标记为候选引入回归。对于由于环境波动产生的不稳定测试，应标记 flaky，而不是随意忽略。

### 18.4 Agent 生成测试

Agent 可以生成复现测试，但应遵循：

- 生成测试不能替代官方隐藏评测。
- 测试必须独立于具体实现细节，尽量验证行为。
- 生成测试和产品 patch 分开保存。
- 默认不把生成测试纳入最终提交，除非任务要求且审查通过。
- 记录测试是在看到哪个证据后生成的。

---

## 19. Runtime 与安全模型

### 19.1 威胁模型

系统需要防范：

- Agent 误删工作区或宿主机文件。
- 恶意仓库在安装或测试时执行危险脚本。
- 命令访问 API key、SSH key 或其他用户数据。
- 子进程在 timeout 后继续运行。
- 符号链接和路径规范化导致目录逃逸。
- 网络下载带来供应链风险和不可复现性。
- 容器资源耗尽影响宿主机。
- Agent 读取 gold patch、隐藏测试或评测密钥。

### 19.2 默认安全策略

- 正式运行使用容器。
- 默认禁用网络。
- 容器使用非 root 用户。
- 不挂载 Docker socket。
- 不挂载用户主目录和凭据目录。
- 只挂载任务工作区和必要只读资源。
- 限制 CPU、内存、PID 和执行时间。
- 高风险命令需要拒绝或在交互模式中确认。
- 环境变量采用允许列表传入。
- 日志自动脱敏。

### 19.3 命令策略

命令策略不应只依靠危险字符串正则。防线应由容器隔离、权限限制、挂载限制、网络策略、资源限制和命令审计共同组成。

命令分类：

- 低风险只读命令：搜索、查看、状态检查。
- 普通开发命令：测试、lint、构建。
- 写入命令：格式化、生成文件、包安装。
- 高风险命令：删除、权限修改、网络、系统管理。

自主模式默认只允许前三类中的受控子集。

### 19.4 超时处理

超时必须终止整个进程组，等待短暂清理，并确认没有残留子进程。超时结果包含部分 stdout/stderr、运行时长和终止原因。超时不等价于测试失败，应单独统计。

---

## 20. SWE-bench 集成设计

### 20.1 集成原则

PatchFlow 不重新实现 SWE-bench 的最终评分逻辑。项目通过 adapter 读取任务、运行 Agent、导出标准预测，再调用官方 Evaluation Harness。

### 20.2 两阶段隔离

#### Inference Phase

Agent 可见：

- `problem_statement`。
- 基础提交中的代码。
- 仓库已有公开测试。
- 公开环境说明。
- Agent 自己创建的临时复现测试。

Agent 不可见：

- gold patch。
- 官方隐藏测试补丁。
- FAIL_TO_PASS 与 PASS_TO_PASS 的答案信息。
- 其他可能泄漏正确修复的 artifact。

#### Evaluation Phase

Agent 已经停止，只提交：

- `instance_id`。
- 模型与方法标识。
- 最终 `model_patch`。

官方 Harness 在独立容器中应用 patch、运行评测测试并生成 resolved 结果。最终隐藏评测结果不得反馈给同一次 Agent 继续修改。

### 20.3 SWE-bench Adapter 职责

- 加载指定数据集和实例。
- 转换为 TaskSpec。
- 准备基础提交或任务镜像。
- 调用 PatchFlow Agent Harness。
- 提取相对于基础提交的 diff。
- 输出官方预测格式。
- 启动或提示启动官方评测。
- 解析结果和日志。
- 区分 Agent failure、patch failure、evaluation failure 和 infrastructure failure。

### 20.4 Adapter 不负责的内容

- 不实现 Agent 推理策略。
- 不修改官方 gold patch。
- 不自行重新定义 resolved。
- 不把隐藏测试暴露给 Agent。
- 不用内部公开测试结果替代最终官方评分。

### 20.5 评测缓存

每次正式评测使用唯一 run ID。项目自身缓存键应考虑任务、模型、Agent 配置、代码版本和 patch 摘要，避免不同 patch 错误复用旧结果。

### 20.6 资源策略

SWE-bench Docker 镜像可能消耗大量磁盘、内存和构建时间。开发期间只使用少量实例；正式实验预先检查磁盘、镜像和并发限制。基础设施失败必须与 Agent 失败分开统计。

---

## 21. 自建 MicroSWE 任务集

### 21.1 目的

MicroSWE 用于快速开发和持续集成，不用于替代 SWE-bench。它应在几分钟内完成小规模回归，而无需构建大量第三方镜像。

### 21.2 规模与组成

V1 目标为 30 至 50 个 Python 任务，覆盖：

- 边界条件错误。
- 空值和类型错误。
- 参数校验错误。
- 状态更新错误。
- 序列化与解析错误。
- 多文件调用契约错误。
- 异常类型或错误消息错误。
- 缓存失效错误。
- 简单并发或异步错误。
- 配置优先级错误。

### 21.3 每个任务的资产

- 独立仓库或固定仓库快照。
- 基础提交。
- Issue 描述。
- 至少一个 FAIL_TO_PASS 测试。
- 若干 PASS_TO_PASS 测试。
- gold patch，仅供评测和定位指标使用。
- 难度和错误类型标签。
- 环境构建说明。

### 21.4 数据质量要求

- Issue 不直接泄漏修改行。
- gold patch 可以稳定应用。
- 修复前测试状态可复现。
- 修复后所有要求测试通过。
- 不依赖不可控网络资源。
- 至少由一次人工审查确认问题描述与测试一致。

---

## 22. Baseline 设计

### 22.1 B0：One-shot Patch

模型一次性接收 Issue 和固定预算的仓库上下文，直接输出 patch。过程中不执行工具和测试。

目的：测量交互、工具和反馈循环相对于一次生成的价值。

### 22.2 B1：Linear ReAct

模型在同一线性历史上循环执行搜索、读取、修改和测试，没有显式阶段、Evidence Graph 和候选分支。

目的：作为常见 Agent scaffold 基线。

### 22.3 B2：Bash-only

模型只通过受控 Shell 与环境交互，使用线性历史，不提供细粒度结构化代码工具。

目的：验证复杂 ACI 是否真正优于强模型直接使用终端。

### 22.4 B3：Agentless-style Pipeline

可选强基线。采用分层定位、批量候选修复和验证重排，但不进行长程 ReAct。

目的：比较交互式 Agent 与阶段化 pipeline 的成本和效果。

### 22.5 Full PatchFlow

显式状态机、Hybrid Localization、Evidence Graph、Candidate Branching 和 Verifier Pyramid 全部开启。

---

## 23. 消融实验设计

### 23.1 ACI 消融

比较 Bash-only、结构化工具和混合工具。保持模型、任务、总 token 和时间预算一致。

### 23.2 定位消融

- 只有 Issue lexical。
- Issue lexical + 文本搜索。
- 加入 traceback。
- 加入 AST / Symbol Graph。
- 完整 Hybrid Localization。

指标重点：File Recall@K、Function Recall@K、读取文件数、定位 token 和最终 resolve rate。

### 23.3 Memory 消融

- Raw conversation。
- 滚动摘要。
- 结构化 Attempt Memory。
- Evidence Graph。

指标重点：重复动作率、被反驳方案重复率、上下文 token 和长任务成功率。

### 23.4 Candidate Branching 消融

候选数设为 1、2、4，比较成功率、成本和墙钟时间。并发实验还需报告峰值资源。

### 23.5 Verification 消融

- 仅目标测试。
- 目标测试 + 相关回归。
- 完整验证金字塔。
- 完整验证 + 防投机规则。

指标重点：表面通过率、官方 resolved、回归率和误选候选比例。

### 23.6 Reflection 消融

- 无 Reflection，测试失败后直接继续生成。
- 自由文本 Reflection。
- Evidence-grounded Reflection。

指标重点：第二轮修复增益、重复失败和额外成本。

---

## 24. 实验公平性要求

所有方法比较必须尽可能控制以下变量：

- 相同任务集合和顺序。
- 相同模型与模型版本。
- 相同温度和采样参数。
- 相同最大 token、调用、工具和时间预算。
- 相同基础提交和容器环境。
- 相同公开测试可见性。
- 相同最终 Evaluation Harness。
- 相同并发级别，或明确报告差异。

如果方法本身需要不同预算，例如 N=4 候选，应同时提供：

- 等预算比较。
- 等策略默认预算比较。
- 成功率与成本的 Pareto 曲线。

不能用更强模型运行完整方法、用更弱模型运行 baseline 后声称架构更优。

---

## 25. 指标体系

### 25.1 最终效果指标

- **Resolve Rate**：官方判定成功任务数除以有效评测任务数。
- **Patch Apply Rate**：成功应用 patch 的比例。
- **Public Validation Pass Rate**：通过内部公开验证的比例。
- **Regression Rate**：引入 PASS_TO_PASS 失败的比例。

### 25.2 定位指标

- File Recall@1、@3、@5。
- Function Recall@1、@3、@5。
- 首次命中相关文件所需步骤。
- 相关文件读取占全部读取的比例。

### 25.3 效率指标

- 平均和中位模型调用次数。
- 输入、输出和总 token。
- API 费用。
- 工具调用次数。
- 测试执行次数和总测试时长。
- Agent 墙钟时间。
- 首个有效 patch 时间。

### 25.4 过程质量指标

- 重复搜索率。
- 重复文件读取率。
- 已反驳方案重复率。
- 无效工具调用率。
- patch 语法失败率。
- 候选淘汰层级分布。
- Reflection 后恢复成功率。

### 25.5 可靠性指标

- 基础设施失败率。
- 模型响应解析失败率。
- Runtime 超时率。
- 多次运行结果方差。
- flaky 测试比例。

### 25.6 统计报告

- 对 resolve rate 提供置信区间。
- 对成对任务结果可使用 McNemar 检验或成对 bootstrap。
- 对 token、成本和时延同时报告均值、中位数和分位数。
- 随机采样方法尽量运行多个种子。
- 小样本结果避免使用夸大的“显著领先”措辞。

---

## 26. 失败分类体系

每个未解决任务必须归入一个主失败类型，并可有多个次级标签。

### 26.1 理解失败

- 误解 Issue 行为。
- 忽略明确约束。
- 无法区分预期与实际行为。

### 26.2 复现失败

- 运行了错误测试。
- 环境无法构建。
- 隐藏条件无法在公开环境中复现。

### 26.3 定位失败

- 未读取 gold 修改文件。
- 命中文件但未命中关键符号。
- 被 traceback 表面位置误导。

### 26.4 修复失败

- 根因假设错误。
- patch 语法错误或无法应用。
- 只修复症状。
- 多文件契约修改不完整。

### 26.5 验证失败

- 未运行关键测试。
- 目标测试通过但产生回归。
- 选择了错误候选。
- 被 flaky 或环境错误误导。

### 26.6 控制失败

- 重复循环。
- 上下文丢失关键事实。
- 预算分配不合理。
- 过早终止或无法终止。

### 26.7 基础设施失败

- 镜像构建失败。
- 依赖下载失败。
- Docker 异常。
- 磁盘、内存或 PID 耗尽。

基础设施失败不得直接计入模型能力失败，报告中应同时给出 raw resolve rate 和排除明确基础设施错误后的诊断指标。

---

## 27. 数据存储与 Artifact 设计

每次运行建议保存：

```text
runs/<run_id>/
├── manifest.json
├── config.snapshot.*
├── task.json
├── trajectory.jsonl
├── evidence_graph.json
├── contexts/
├── model_calls/
├── tool_calls/
├── candidates/
├── final.patch
├── internal_report.json
├── evaluation_report.json
└── logs/
```

原则：

- 小型结构化数据可放 SQLite 或 JSONL。
- 大型 stdout、模型原文和测试报告独立保存，事件只存引用和摘要。
- patch、配置和报告使用可读文本格式。
- API key、认证头和敏感环境变量不得写入 artifact。
- schema 必须带版本号，未来迁移时保留兼容工具。

---

## 28. 可观察性与轨迹浏览器

### 28.1 CLI 实时信息

运行时应显示：

- 当前任务和阶段。
- 已使用与剩余预算。
- 当前主假设。
- 最近工具和结果摘要。
- 候选数量及验证状态。
- 当前 token、费用和时间。

### 28.2 轨迹浏览器

V1 可以先生成静态 HTML 或结构化终端报告，不必开发复杂前端。浏览器至少支持：

- 按状态阶段折叠。
- 查看模型动作和工具 Observation。
- 查看每轮上下文摘要。
- 查看 Evidence Graph 变化。
- 对比候选 diff 和验证结果。
- 跳转到失败步骤。

### 28.3 实验仪表板

最小报告应包括：

- 配置摘要。
- 总体指标。
- 每任务结果表。
- 成本与成功率图。
- 失败类型分布。
- 定位指标。
- baseline 与消融对比。

---

## 29. 配置设计

配置分层：

1. 项目默认配置。
2. Agent 策略配置。
3. 模型配置。
4. Runtime 与资源配置。
5. 数据集配置。
6. 单次实验覆盖参数。

配置原则：

- 所有影响实验的参数都可序列化。
- 密钥只通过环境或安全密钥系统注入，不写入配置快照。
- 配置加载后进行完整校验。
- 未知字段默认报错，避免拼写错误被忽略。
- 正式 run 保存解析后的最终配置，而不只保存用户输入片段。

---

## 30. 并发与调度

存在两级并发：

- **任务级并发**：批量评测同时运行多个独立任务。
- **候选级并发**：同一任务同时验证多个候选。

调度器需要限制：

- 最大容器数量。
- 最大模型并发请求。
- 每模型 provider 的速率和 token 限额。
- 最大测试并发。
- 宿主机 CPU、内存和磁盘余量。

调度原则：便宜验证优先，完整回归限流。任务取消时，应取消未开始候选并终止正在运行的受控进程。

---

## 31. 错误处理原则

错误分类至少包括：

- ValidationError：输入或配置无效。
- ModelError：模型服务或响应错误。
- ToolError：工具参数或工具逻辑错误。
- PolicyDenied：权限策略拒绝。
- RuntimeError：环境执行失败。
- TimeoutError：命令、模型或任务超时。
- PatchError：补丁生成或应用失败。
- VerificationError：验证流程无法完成。
- InfrastructureError：Docker、磁盘或依赖基础设施异常。

错误处理要求：

- 不使用模糊的“something went wrong”。
- 用户可操作错误给出下一步建议。
- 可恢复错误由上层策略决定是否重试。
- 重试不能掩盖持续性错误。
- 每次重试计入预算并写入事件流。
- 终止报告保留原始根因和因果链。

---

## 32. 测试策略

### 32.1 单元测试

重点覆盖：

- 路径解析和目录逃逸。
- 工具输入校验。
- 输出截断。
- 超时和进程终止。
- 状态机合法迁移。
- 预算扣减。
- Evidence Graph 一致性。
- 上下文选择与 token 限制。
- patch 统计和候选排序。
- 指标计算和失败分类。

### 32.2 组件测试

- 使用 fake model 驱动确定性 Agent 轨迹。
- 使用临时 Git 仓库测试 patch、回滚和 worktree。
- 使用小型 Docker 镜像测试 Runtime 隔离。
- 使用固定 pytest 项目测试验证金字塔。

### 32.3 集成测试

- 一个成功修复任务。
- 一个定位错误后反思成功的任务。
- 一个所有候选失败的任务。
- 一个命令超时任务。
- 一个模型结构化输出损坏任务。
- 一个候选并发隔离任务。
- 一个任务取消和资源清理任务。

### 32.4 端到端测试

- MicroSWE 小型固定集合。
- 至少一个 SWE-bench gold patch 环境验证。
- 至少一个由 Agent 生成预测并交给官方 Harness 的完整闭环。

### 32.5 回归测试

关键 Agent 行为变化必须运行固定 smoke subset。模型结果可能随机时，至少验证结构性不变量，例如事件完整、预算不超限、工作区隔离和结果可解析。

---

## 33. 开发里程碑

### 第 1 周：项目规格与领域骨架

目标：建立稳定边界，不接真实模型也能描述完整任务。

交付物：

- 项目 README 初稿和本文档定稿。
- TaskSpec、AgentState、Event、Tool、Runtime 等领域接口设计。
- 配置、run ID 和 artifact 约定。
- ADR 模板。
- 基础测试框架。

验收：能够用 fake 对象创建任务、记录事件并生成空运行报告。

### 第 2 周：Runtime 与工具闭环

交付物：

- LocalRuntime。
- DockerRuntime 最小版本。
- search、read、patch、diff、test 和 command 工具。
- timeout、路径限制、输出截断和日志。

验收：在临时仓库中完成修改、测试、回滚和 diff 导出，安全边界测试通过。

### 第 3 周：Baseline Agent

交付物：

- 模型抽象与至少一个 provider adapter。
- One-shot、Linear ReAct、Bash-only。
- 全局预算和终止条件。
- 轨迹记录。

验收：三个 baseline 能在同一批 MicroSWE 任务上运行并输出统一报告。

### 第 4 周：仓库索引与 Hybrid Localization

交付物：

- 仓库地图。
- Python AST 符号索引。
- Issue、搜索、traceback 和符号证据融合。
- 定位评测指标。

验收：能够输出 Top-K 文件和函数及其证据，并完成定位消融。

### 第 5 周：显式状态机与 Evidence Graph

交付物：

- 完整阶段状态机。
- Evidence Graph 节点、边和溯源。
- ContextBuilder。
- 结构化 Reflection。

验收：失败候选能够更新假设，后续上下文不再重复被反驳方案。

### 第 6 周：候选分支与验证金字塔

交付物：

- 独立候选工作区。
- 候选并发验证。
- 分阶段测试选择。
- 硬拒绝和最终选择规则。

验收：候选互不污染，能够正确淘汰语法失败、目标测试失败和回归候选。

### 第 7 周：SWE-bench Adapter

交付物：

- SWE-bench TaskSpec 转换。
- 预测文件导出。
- 官方 Harness 调用与结果解析。
- inference / evaluation 隔离检查。

验收：先用 gold patch 验证环境，再完成至少 5 个 Agent 预测的端到端评测。

### 第 8 周：任务集与正式实验

交付物：

- MicroSWE-30 或更大任务集。
- 固定 SWE-bench 子集。
- baseline 和主要消融运行。
- 统计报告和失败分类。

验收：所有实验具有唯一配置和可追溯 artifact，结果能够重新聚合。

### 第 9 周：可观察性与展示

交付物：

- 轨迹浏览器或静态报告。
- 成本与成功率图表。
- 成功和失败案例分析。
- 交互 CLI 打磨。

验收：陌生使用者能根据 README 运行一个示例并理解轨迹。

### 第 10 周：文档、发布与面试准备

交付物：

- 完整 README 和架构图。
- 实验报告。
- 演示视频或 GIF。
- 设计决策与已知限制。
- 简历描述和面试问答。

验收：仓库达到可公开状态，结果不依赖开发者本机隐式配置。

---

## 34. MVP、V1 与 V2 范围

### 34.1 MVP

- Python 仓库。
- LocalRuntime 和基础 DockerRuntime。
- 核心工具。
- Linear ReAct。
- 统一轨迹。
- 单候选 patch。
- 语法、目标测试和 diff 输出。
- 10 个 MicroSWE 任务。

MVP 的目标是“可信地跑通”，不追求前沿亮点。

### 34.2 V1

- 三个主要 baseline。
- 显式状态机。
- Hybrid Localization。
- Evidence Graph。
- 2 至 4 个候选分支。
- Verifier Pyramid。
- SWE-bench Adapter。
- 30 至 50 个 MicroSWE 任务。
- 25 至 50 个 SWE-bench 固定子集实验。
- 轨迹浏览器和完整实验报告。

V1 是用于投递实习的目标版本。

### 34.3 V2

- 多语言 parser 和 Runtime。
- MCP 与 Skills。
- 历史轨迹检索。
- 学习型定位排序器。
- 训练 patch verifier 或 process reward model。
- SFT / DPO / RL 数据管线。
- 可选 Reviewer Agent。
- 更大规模动态或污染更低的评测集。

---

## 35. 风险与缓解措施

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| 项目范围过大 | 长期无法形成可运行版本 | 先完成 MVP，每个亮点独立开关 |
| API 成本过高 | 无法完成消融 | MicroSWE 开发、小子集筛选、缓存索引、固定预算 |
| SWE-bench 环境过重 | 磁盘和构建时间不足 | 从少量实例开始，限制并发，必要时使用云评测 |
| 模型随机性大 | 实验结论不稳定 | 固定参数、多种子、成对比较和置信区间 |
| gold patch 不是唯一解 | 定位指标误导 | 将定位 Recall 视为诊断指标，不替代 resolved |
| Agent 修改测试作弊 | 虚假成功 | 路径策略、测试 diff 审计、独立最终评测 |
| 测试通过但需求未满足 | verifier 误判 | 多层验证、行为复现、失败案例人工分析 |
| 多候选成本迅速增长 | 性价比差 | 便宜验证早停、动态候选数、报告 Pareto 曲线 |
| Evidence Graph 过度复杂 | 实现成本高于收益 | 首版只保留任务相关节点和简单查询 |
| 容器仍不绝对安全 | 宿主风险 | 非 root、无网络、资源限制、最小挂载、不暴露 socket |
| Benchmark 污染 | 结果不能代表泛化 | 自建新任务、报告限制、避免训练数据与评测重叠 |
| 功能很多但没有结论 | 面试亮点模糊 | 每个核心模块绑定明确假设和消融 |

---

## 36. 架构决策记录建议

至少为以下决策建立 ADR：

1. 为什么核心 Agent 不直接依赖 LangChain。
2. 为什么使用显式状态机而不是完全自由 ReAct。
3. 为什么 Evidence Graph 与原始轨迹分开。
4. 为什么最终评分复用官方 SWE-bench Harness。
5. 为什么正式执行默认使用 Docker。
6. 为什么候选之间使用独立工作区。
7. 为什么确定性验证优先于 LLM Judge。
8. 为什么 V1 聚焦 Python。
9. 为什么默认不采用多 Agent。
10. 为什么工具数量保持克制，并保留 Bash-only baseline。

每个 ADR 包含背景、候选方案、最终选择、后果和未来重新评估条件。

---

## 37. 面试深挖问题清单

项目完成后，开发者应能够回答：

### Agent 与控制

- 为什么需要显式状态机？
- ReAct、pipeline 和树搜索分别适合什么场景？
- Agent 如何判断应该继续尝试还是停止？
- Reflection 如何避免变成无效自然语言？

### 工具与 ACI

- 为什么 `read_code` 不直接返回完整文件？
- 结构化工具和 Bash-only 各有什么优缺点？
- 工具 Observation 如何截断而不丢关键错误？
- 为什么 patch 比整文件重写更适合 Agent？

### 定位与上下文

- traceback 为什么可能误导根因定位？
- AST 调用图有哪些静态分析局限？
- Evidence Graph 比摘要多解决了什么问题？
- 如何量化上下文策略是否有效？

### 验证

- 测试通过为什么不等于 Issue 已解决？
- 如何避免 Agent 通过修改测试作弊？
- 如何选择目标测试和回归测试？
- LLM Judge 为什么不能覆盖确定性测试失败？

### 系统与安全

- 正则命令黑名单为什么不够？
- 如何确保 timeout 能终止子进程？
- 候选并发如何防止状态污染？
- 如何避免 Docker 镜像和结果缓存影响实验？

### 实验

- 为什么必须固定模型与预算？
- 子集结果为什么不能直接对比完整排行榜？
- resolve rate 之外还应观察哪些指标？
- 如何区分 Agent 失败和基础设施失败？

### 后训练扩展

- 如何从轨迹构造 SFT 数据？
- 如何从多个候选构造 DPO 偏好对？
- 测试奖励有哪些 reward hacking 风险？
- process reward 应该监督哪些步骤？

---

## 38. 后训练与数据飞轮扩展

V1 完成后，轨迹系统可以支持以下研究：

### 38.1 SFT 数据

从成功轨迹中提取状态、上下文、合理动作和工具结果。需要过滤偶然成功、冗余动作和潜在泄漏，不能简单把全部成功轨迹当作高质量示范。

### 38.2 偏好数据

对同一状态下的多个候选，根据确定性验证结果构造偏好对。偏好依据应保存，例如目标测试通过、回归失败或 patch 无法应用。

### 38.3 Verifier 训练

训练模型预测候选补丁通过后续验证的概率，输入可包括 Issue、相关代码、patch 和早期验证结果。评测应关注排序质量、校准和不同仓库泛化。

### 38.4 Process Reward

过程奖励可围绕以下中间目标：

- 是否定位到相关文件。
- 是否生成有效复现。
- 是否提出可检验假设。
- 是否避免重复无效动作。
- 是否生成可应用 patch。
- 是否通过逐层验证。

任何学习型奖励都必须与独立最终测试比较，防止只优化代理指标。

### 38.5 数据飞轮

推荐闭环：

1. 运行 Agent 产生多样轨迹。
2. 用执行结果标记成功、失败和中间状态。
3. 过滤泄漏、环境错误和低质量轨迹。
4. 构造 SFT、偏好或 verifier 数据。
5. 训练小模型或适配器。
6. 在严格隔离的任务集上重新评测。
7. 分析新模型带来的新型 reward hacking。

---

## 39. README 与发布要求

公开仓库 README 至少包含：

- 一句话项目定位。
- 系统架构图。
- 30 秒运行示例。
- 一个完整任务演示。
- 与 baseline 的实验表格。
- SWE-bench 子集和评测说明。
- 安全警告与沙箱边界。
- 项目结构和扩展点。
- 复现实验步骤。
- 已知限制。
- 路线图。

发布前检查：

- 不提交 API key、token、私有仓库和用户路径。
- 不提交无法再生成的大型临时 artifact。
- 锁定核心依赖版本。
- 提供许可证和引用信息。
- 至少在一台干净环境验证安装。
- 示例结果标明模型、日期、预算、任务集合和 commit。

---

## 40. 项目验收清单

### 40.1 P0 验收

- [ ] TaskSpec 可以统一表示本地与 benchmark 任务。
- [ ] Agent 通过统一 Runtime 工作。
- [ ] 所有工具结构化校验并记录事件。
- [ ] 命令具备超时、输出限制和工作区限制。
- [ ] One-shot、Linear ReAct、Bash-only 可运行。
- [ ] 能输出标准 Git diff。
- [ ] 能保存完整轨迹和 run manifest。
- [ ] 能在 MicroSWE 上批量运行。
- [ ] 能导出 SWE-bench prediction。
- [ ] 能调用官方 Harness 获得最终结果。

### 40.2 P1 验收

- [ ] Hybrid Localization 有可解释排名和 Recall@K。
- [ ] Evidence Graph 能追踪事实、假设、反证和尝试。
- [ ] ContextBuilder 遵守 token 预算并记录选择结果。
- [ ] 候选补丁在独立环境中生成和验证。
- [ ] Verifier Pyramid 能逐层淘汰候选。
- [ ] 完成主要 baseline 与消融。
- [ ] 报告成功率、成本、时延和失败类型。
- [ ] 至少分析 10 个代表性案例。
- [ ] README 和架构文档可供陌生开发者复现。

### 40.3 安全验收

- [ ] 路径遍历和符号链接逃逸测试通过。
- [ ] 容器不暴露宿主 Docker socket 和凭据。
- [ ] 默认禁用网络。
- [ ] 进程 timeout 后无残留。
- [ ] 候选工作区相互隔离。
- [ ] Agent 无法读取 gold patch 和隐藏测试。
- [ ] 日志经过敏感信息脱敏。

---

## 41. 推荐的首轮实验矩阵

为了控制成本，首轮正式实验建议采用一个固定的 30 题子集，并按以下顺序运行：

| 实验 | 方法 | 目的 |
|---|---|---|
| E0 | One-shot | 最低交互基线 |
| E1 | Linear ReAct | 标准 Agent 基线 |
| E2 | Bash-only | 极简 ACI 基线 |
| E3 | ReAct + Hybrid Localization | 测试定位收益 |
| E4 | E3 + Evidence Graph | 测试结构化记忆收益 |
| E5 | E4 + 2 Candidates | 测试小规模搜索收益 |
| E6 | E5 + Verifier Pyramid | 完整 PatchFlow |
| E7 | E6，4 Candidates | 测试 inference-time scaling |

首轮只在结果显示模块确有收益后扩大任务数量。若某模块没有收益，应优先分析原因，而不是隐藏结果或继续叠加更多模块。

---

## 42. 默认设计决策

在没有新证据前，项目采用以下默认选择：

- 语言：Python。
- 核心框架：自研轻量 Agent Harness。
- 数据校验：Pydantic。
- 内部不可变结果：dataclass 或等价类型。
- CLI：轻量命令行框架加 Rich 输出。
- 并发：asyncio 管理 I/O，并对阻塞命令使用受控执行器。
- 索引：Python AST 起步，后续评估 tree-sitter。
- 图结构：内存图加可序列化存储，首版不引入图数据库。
- 元数据存储：SQLite。
- 轨迹存储：JSONL 加大型 artifact 文件。
- 沙箱：Docker。
- 编辑协议：统一 diff patch。
- 最终评分：官方 SWE-bench Evaluation Harness。
- 多 Agent：默认关闭。
- 网络：默认关闭。
- 候选数量：开发阶段 1，V1 默认 2，实验比较 1/2/4。

---

## 43. 参考项目与资料

- Mini Claude Code：交互式 CLI、MCP、Skills、上下文压缩和工具模块化参考。  
  https://github.com/scipenai/mini-claude-code

- SWE-agent：Agent-Computer Interface、仓库级交互和 SWE-bench Agent 参考。  
  https://github.com/SWE-agent/SWE-agent

- mini-SWE-agent：Bash-only、线性轨迹和极简强基线参考。  
  https://github.com/SWE-agent/mini-swe-agent

- Agentless：分层定位、候选修复和验证重排参考。  
  https://github.com/OpenAutoCoder/Agentless

- SWE-bench：真实 GitHub Issue 任务和官方 Docker Evaluation Harness。  
  https://github.com/SWE-bench/SWE-bench

- SWE-Gym：软件工程 Agent 训练环境、轨迹和 verifier 研究参考。  
  https://arxiv.org/abs/2412.21139

- SWE-smith：可扩展软件工程任务生成和数据构造参考。  
  https://arxiv.org/abs/2504.21798

---

## 44. 最终项目定位声明

PatchFlow 不是一个只展示大模型 API 和工具调用的 Demo，也不是对某个商业编程助手界面的简单复刻。

它应当成为一个具备以下特征的仓库级 Code Agent：

- 能在真实代码环境中行动。
- 能用可追溯证据定位和修复问题。
- 能在隔离环境中搜索多个候选方案。
- 能通过确定性验证约束模型行为。
- 能记录完整轨迹并转化为训练数据。
- 能通过标准 benchmark、baseline 和消融证明设计价值。
- 能清楚说明成功、失败、成本、安全边界和已知限制。

项目最终最重要的成果不是代码量，而是一条完整可信的证据链：

> 问题是什么，系统为什么这样设计，实现是否可靠，实验是否公平，模块是否真正带来收益，以及失败时我们学到了什么。

